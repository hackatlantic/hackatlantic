
  # Hack Atlantic Landing Page

  This is a code bundle for Hack Atlantic Landing Page. The original project is available at https://www.figma.com/design/K27taPLdBvgV8ogrvO0HeF/Hack-Atlantic-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  